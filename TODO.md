# CHANGELOG

> GitHub issues are the source of truth for tasks (`todo` / `in-progress` / `review` / `done`).
> This file is a completed-work log only — do not add new checkboxes here.
> Open backlog (2026-09-05): issues #3–#7 (notifications, onboarding, background-audio decision, audio matrix, missing audio data).

## Completed

- [x] **Offline Support** — All data is bundled locally (JSON + fonts), no network calls exist; the app works fully in airplane mode.

- [x] **Internationalization (i18n)** — All Arabic UI strings extracted into `src/i18n/ar.ts`; a `t()` helper in `src/i18n/index.ts` serves lookups and is ready for additional language files.

- [x] **Settings Button on Home Screen** — Gear icon added to the `CategoriesScreen` navigation header via `useLayoutEffect`; tapping it navigates directly to Settings.

- [x] **Phrase Pager Swipe Navigation (Refactored)** — Replaced the buggy 3-card `PanGestureHandler`/`Animated` carousel (which flickered: it animated cards then swapped content underneath, and `useNativeDriver` was off) with a virtualized horizontal `FlatList` pager — one page per phrase, `pagingEnabled`, pages sized to the phrase area, vertical scroll per page for long text. RTL swipe direction (swipe right = next) is guaranteed on iOS/Android (native `forceRTL`) and web (`scaleX: -1` mirror); bidirectional Redux ↔ pager index sync via `expectedIndexRef` covers reset, counter-complete, audio auto-advance and saved-index restore without loops or flicker.

- [x] **Code Cleanup & Lint** — ESLint (`eslint-config-expo`) + Prettier configured; `npm run lint` / `npm run lint:fix` added; all errors resolved (including refactoring Animated values from `useRef` to `useMemo`); redundant JSX section comments removed across all screens.

- [x] **Relocate Settings Button & Add Reset Progress** — Move the Settings gear icon from the zikr (CategoryScreen) header to the home (CategoriesScreen) header as a fixed button. Remove it from the zikr header and replace it with a "Reset" button (e.g. `refresh-outline` / `restart` icon) that resets the current zikr's progress back to zero.

- [x] **UI Redesign (all screens)** — Restyle all screens to the former reference design (`adhkar-redesign.html`, since removed from the repo): warm sand background (`#F4EEE0`), deep emerald primary (`#2F5D50`), gold accent (`#BB9A4F`), card surfaces (`#FBF7ED`), rounded category cards with icon chip + count pill, gradient verse banner on home, gold ring counter on the zikr screen, large circular tasbih button with breathing animation, and grouped settings cards. All 3 themes (light / solarized / dark) share the same visual structure with adapted colour palettes.

- [x] **Favourite Categories** — Add a favourite (star/heart) toggle button to each category card; persist favourited category IDs in the store, and sort the home screen list so favourited categories appear first in order, and replace it with count of zikr category number

- [x] **Seamless Navigation Header** — Remove the visible separator between the native header bar and the screen content: set `headerTransparent: true` (or match header background to `bgColor`), hide the bottom border/shadow, and blend the title/back-button tint into the page so the header feels like a natural part of each screen rather than a floating toolbar.

- [ ] **Complete Media Audio Playback** — Finish and verify audio playback using the dataset `audio` / `filename` fields as the source of truth, with all audio clips bundled locally inside the app (no remote CDN).
	- [x] Add `expo-audio` playback, originally with remote MP3 loading and local cache support.
	- [x] Add the Settings controls for enabling audio and auto-playing the next zikr; persist both preferences.
	- [x] Add play/pause, loading, replay-after-finish, and player cleanup behavior.
	- [x] Make auto-play-next stop at the final phrase and replace the current player before starting the next phrase.
	- [x] Handle navigation and loading races so stale operations cannot attach to another phrase or block future playback.
	- [x] Normalize empty audio metadata and support phrase-level/category-level source fallback.
	- [x] Show separate themed feedback for missing audio (`noAudio`) and playback errors (`audioError`), with retry support.
	- [x] Refactor `src/audio/audioSource.ts` to resolve audio to `{ kind: 'local'; filename: string } | { kind: 'missing' }` instead of remote URLs.
	- [x] ~~Refactor `src/audio/audioCache.ts` into a legacy-cache cleanup helper; remove remote download / `expo-file-system` cache logic.~~ Removed: legacy cache cleanup no longer needed.
	- [x] Create the static `AUDIO_ASSETS` map (inline in `src/audio/audioSource.ts`) with a `Record<filename, require(...)>` resolver for bundled MP3 URIs via `expo-asset`.
	- [x] Update `src/audio/useZikrAudio.ts` to load the local asset URI directly into `createAudioPlayer`, dropping network fetch paths.
	- [x] Update `src/config/config.ts` to remove the placeholder CDN `baseUrl` / `cacheDir` and document the local asset path convention.
	- [x] Redesign the zikr reader audio player to match the reference image: play/pause/finished/loading/missing states, progress bar with current/total time, and placement between the zikr text and source info.
	- [x] Track playback `currentTime` / `duration` in the `playback` slice and poll while playing for a live progress bar.
	- [x] Hide the audio player entirely when audio is disabled in Settings or the current phrase has no audio file (`missing`).
	- [x] ~~Add a one-time cache/migration helper to clear any legacy remote-download audio cache from `expo-file-system` so old cached MP3s do not conflict with local assets.~~ Removed along with `src/audio/audioCache.ts`.
	- [x] ~~Clear old audio/filename data from `src/dataset/azkar-sample.json` so the app reports missing audio until new clips are available.~~ Superseded: audio data was cleared at the time, then repopulated — 46 of 132 phrases are now wired (categories `3` + `4` fully; all other categories still report missing audio).
	- [x] Collect the MP3 clips, place them under `assets/audio/`, populate the `AUDIO_ASSETS` map in `src/audio/audioSource.ts`, and fill the `audio` / `filename` fields in `src/dataset/azkar-sample.json` — done for categories `3` + `4` (29 clips: `1`–`14`, `15-16`, `17`–`22`, `24`–`31`; removed obsolete `15.mp3`/`16.mp3`/`23.mp3`).
 	- [ ] Moved to issue #5: Decide whether playback must continue while the app is backgrounded or the screen is locked. If required, configure the Expo Audio session, background playback, Android media notifications, and iOS background audio mode.
 	- [ ] Moved to issue #6: Test Android, iOS, and Web: first play, pause/resume, replay, phrase navigation, auto-play-next, final phrase, and missing audio.
 	- [ ] Moved to issue #7: Future data task: add or intentionally approve missing audio entries in the dataset — 86 of 132 phrases across all categories except `3` and `4` (including categories `1`, `21`, and `122`) currently have empty audio fields.

- [x] **New Category: سنن يوم الجمعة** — Add a new category named `سنن يوم الجمعة` (dataset id `21`) and include its data in the dataset so it appears alongside the other zikr categories.

- [ ] Moved to issue #3: **Notifications** — Add a notification feature that reminds the user twice every day, once in the morning and once in the evening, and also sends a weekly Friday reminder for the `سنن يوم الجمعة` category. Add the notification customization options to the settings page so the user can enable, disable, and adjust the reminder behavior.

- [ ] Moved to issue #4: **First-Launch Onboarding** — Show a one-time walkthrough or intro screen on first app open to guide new users through the app's features and how to use them.

- [x] **Back Button Consistency Across Screens** — Add a back button to all applicable screens and align the header layout so the screen title stays visually centered alongside the back button.

- [x] **Add Hadith on Home Screen** — Display a hadith section on the home screen directly below the ayah block, following the same visual style as the existing home content.

- [x] **Refactor Free Tasbih Layout** — Center the Free Tasbih screen title and keep the counter fixed in a stable on-screen position so it does not shift with other content, and remove the centered word "سبح" and put the number inside the circle, and follow the themes styles.

- [x] fix the theme in home page

- [x] switch the numbers format to be english not hindi

- [x] enhance the color palette of each theme to be look suitable and harmonic:
	- for light theme: use whity and shine color to highlight the text and contrast the borders, hint could use here white and relaxed shiny blue.
	- for solarized theme: use aye comfortable colors, hint could use color here the suitable degree of dark green and gold colors.
	- for dark theme: use dark, matte and aye comfortable colors, hint could be between black and blue

- [x] replace the back button icon to be look at the other side (left)

- [x] fix refreshment zikr when sliding right or left on mobile — the flicker (previous zikr reloading for a few milliseconds) came from the old carousel swapping phrases underneath the animated transform; the `FlatList` pager transitions natively with no content swap, so it is flicker-free.  

- [x] center the category of "مسبحة حرة" at the categories page
 as the text is little bit to right as it has not contained the favorite icon

- [x] **Make Phrase (Zikr) Content Scrollable & User-Friendly on CategoryScreen** — Ensure the zikr phrase text is easy to scroll and comfortable to read for long phrases.
	- Location: `src/components/PhraseCard.tsx` — the per-page vertical `ScrollView` inside each pager page.
	- Ensure `showsVerticalScrollIndicator` gives visual feedback and momentum/bounce scrolling is smooth (`bounces={true}` on iOS).
	- Add comfortable `contentContainerStyle` padding so text never touches the card edges.
	- Add a subtle overflow gradient at the bottom of the visible area when content exceeds the page, so users know there is more text.
	- Consider a "scroll to top" on header tap and/or a small "scroll to end" affordance for very long phrases.
	- Verify tall/extra-long phrase text remains fully reachable on all supported screen sizes.

- [x] **Keep Screen Awake While on Phrase (Category) Screen** — Prevent the device screen from dimming/locking while the user is reading zikr on `CategoryScreen`.
	- Add `expo-keep-awake` via `npx expo install expo-keep-awake`.
	- In `CategoryScreen.tsx`, call `KeepAwake.activateKeepAwakeAsync()` in a `useEffect` on mount and `KeepAwake.deactivateKeepAwake()` on unmount / when navigating away, so the screen stays on only while the user is actively reading.
	- Clean up the keep-awake activation in the effect's return path to avoid leaving the screen awake after leaving the screen.

- [x] **Volume Buttons Navigate Between Zikr Phrases** — Press the hardware volume up/down buttons to move to the next/previous phrase (zikr) while on `CategoryScreen`. A Settings toggle (`volumeNav.enabled`, persisted) lets the user enable/disable the feature; when enabled the native volume UI is hidden and the volume is snapped back so the buttons only navigate, without changing the volume.
	- React Native exposes no volume-button events out of the box; a native library is required: `react-native-key-event` (Android raw key events `KEYCODE_VOLUME_UP` = 24, `KEYCODE_VOLUME_DOWN` = 25) or `react-native-volume-manager` (can intercept volume keys and suppress the system HUD).
	- Register a listener in `CategoryScreen.tsx` in a `useEffect` on mount: volume-up → `setIndexCount(currentIndex + 1)`, volume-down → `setIndexCount(currentIndex - 1)`, respecting phrase boundaries.
	- Guard rapid consecutive presses (held key) with `useTimeGuardedCallback` / `config.interaction` timing to prevent flickering through phrases.
	- Clean up the listener on unmount.
	- Note: intercepting volume buttons on iOS generally requires a custom native module and is usually not feasible; this feature is expected to be Android-only unless a cross-platform solution is found.

- [x] **Volume Nav Disabled by Default** — The volume-button navigate feature should be disabled by default for new installs and fresh data. Change the default in `volumeNavSlice` (`initialState.enabled`) and the `loadPersistedState()` fallback (`volumeNavEnabled`) from `true` to `false`. Existing users who already enabled it keep their persisted preference.
	- Files: `src/store/slices/volumeNavSlice.ts`, `src/store/persistence.ts`.

- [x] **Volume Buttons Decrement Counter Before Switching Zikr** — Change the volume-down button behavior on `CategoryScreen` so it decrements the current phrase's tasbih counter first, and only switches to the next phrase once the counter reaches the phrase's required `count`. Volume-up should switch to the previous zikr without touching the counter. This makes the volume buttons usable as a hands-free tasbih counter.
	- In `CategoryScreen.tsx`, maintain refs for the current phrase's `clicks[index]` and `count` so the volume listener (empty deps) always reads fresh values.
	- Volume-down logic: if `clicks[ currentIndex] < phrase.count` → decrement counter (dispatch + total count increment + animation) and restore volume; if counter is already at the required count → dispatch `incrementIndex()` (switch to next phrase).
	- Volume-up logic: if `currentIndex > 0` → dispatch `setIndexCount(currentIndex - 1)` (go back without touching counter).
	- Keep the existing `volumeNavGuardRef` debounce so rapid holds do not cause race conditions.

- [x] **Volume Button Counter Control on FreeTasbihScreen** — Extend the volume-button feature to the Free Tasbih screen so the user can count hands-free without tapping the screen. Volume-down increments the free tasbih counter (+1 and global total count); volume-up decrements the free tasbih counter (−1, not below 0, does not affect global total count). Respect the same `volumeNav.enabled` Settings toggle.
	- Add a `VolumeManager` listener effect in `FreeTasbihScreen.tsx` mirroring the CategoryScreen pattern.
	- Guard rapid presses and clean up the listener on unmount.
	- Snap volume back and hide the native volume UI while active.

- [x] **Hide Quote Cards While Search Is Open (#8)** — The Quran quote + hadith cards pushed category results behind the open keyboard. Both cards are now conditionally rendered only when search is closed (`!isSearchOpen` in `CategoriesScreen.tsx`); opening search hides them, closing restores them.

- [x] **Volume-Key Reliability Fix (#11, in review)** — Fixed volume-down intermittently doing nothing on `CategoryScreen`/`FreeTasbihScreen`: the system volume is re-centered to mid-range when parked at a min/max rail (where Android fires no event), every swallowed press (restore/guard) re-baselines the direction detector, and `__DEV__`-only `[VolumeNav:*]` logs make failures observable (`adb logcat | grep VolumeNav`). Expo Go confirmed unsupported for this feature — requires a custom dev build.
- [x] **Volume Keys Yield to Audio While Playing (#10)** — On `CategoryScreen`, hardware keys control the system volume normally (native UI shown, no snap-back) while `audioStatus === 'playing'`; nav mode re-engages with a re-synced baseline on pause/finish. `FreeTasbihScreen` verified as no-op (no audio path).

- [x] **Review Azkar Arabic Text Whitespace and Typos (#9)** — Normalized whitespace across `src/dataset/azkar-sample.json` (22 cats, 132 phrases): trimmed per-line leading/trailing spaces, collapsed double spaces/tabs, removed ` \n`/`\n ` artifacts and ` \n\n ` blank-line padding, limited consecutive blank lines to one. Fixed 59 `text`/`subtext` entries (e.g. cat 3 id 4 `مَا فِي  هَذَا` → `مَا فِي هَذَا`, cat 3 id 9 `اللَّهُمَّ  إِنِّي` → `اللَّهُمَّ إِنِّي`, subtext leading space in cat 3 id 21). No safe wording/tashkeel typos beyond whitespace identified (requires external Hisn Al-Muslim verification). Verified `npm run lint`/`npx tsc --noEmit` clean and no `\n\n\n`/double-space remains.

- [x] **Audio Repeat Count Sync with Counter (#21)** — Fixed `src/audio/useZikrAudio.ts:16` to repeat `remainingCount` (`count - clicks`) not full `count`, add `onLoop` per-repetition callback that decrements the footer counter (`CategoryScreen:84` `handleAudioLoop` mirrors `handlePhraseClick`: `setClicks` + `incrementTotalCount` + animation + `incrementIndex` after 300 ms), sync manual taps ↔ audio loops via `remainingCount` prop and `remainingRepeatsRef` effect, and guard duplicate `didJustFinish` within 250 ms plus `seekTo→play` error handling to fix large counts (7/100) stopping after 3–4 loops. Verified `npm run lint:fix`/`npx tsc --noEmit` clean; pager `expectedIndexRef` loop-free sync, `missing` hides player, volume keys still yield to `audioActive` per #10.

- [x] **Notification Infrastructure + Permissions (#13)** — Bootstrap local-notification foundation with `@notifee/react-native@9.1.8` (requires dev build/prebuild, Expo Go gracefully no-ops). Added `src/notifications/channels.ts:1` (`adhkar-reminders` HIGH channel, single shared channel), `src/notifications/notifeeService.ts:1` (guarded `require` import, `ensureAdhkarChannel` idempotent, permission `get/request/openSettings`, exact-alarm + battery-optimization helpers with inexact-AlarmManager ~15 min Doze fallback), `src/notifications/permissions.ts:1` (`useNotificationPermissions` hook with AppState refresh, `checkExactAlarmAvailable`), `src/components/PermissionBlockedBanner.tsx:1` (themed `getAzkarTheme()` + `AZKAR_PRIMARY_FONT`, RTL `textAlign:right`, `useTimeGuardedCallback` + `config.interaction.navButtonGuardMs`), i18n keys `notifPermissionTitle/Body/OpenSettings/Request` + `exactAlarmRationale` in `src/i18n/ar.ts:1`, and `App.tsx:1` channel bootstrap on `appStore` ready (no scheduling yet). Updated `app.json:1` Android permissions `POST_NOTIFICATIONS`, `SCHEDULE_EXACT_ALARM`, `USE_EXACT_ALARM`, `RECEIVE_BOOT_COMPLETED`, `FOREGROUND_SERVICE`, `FOREGROUND_SERVICE_MEDIA_PLAYBACK`. Verified `npm run lint:fix` + `npx tsc --noEmit` clean; offline-first, web/Expo Go no crash.

- [x] **Adhkar Time Configuration (#14)** — Added persisted `reminders` slice `src/store/slices/reminderSlice.ts:1` (`morning {enabled, time {hour,minute}}`, `evening {enabled, time {hour,minute}}`, defaults 06:00/17:00, `clampTime`, 7 actions + `REMINDER_DEFAULTS`) with 3-place wiring `src/store/persistence.ts:1` (`isAnyOf` listener → `adhkarReminders` + `loadPersistedState` read) and `src/store/index.ts:1` (`reminders` reducer), `src/config/config.ts:1` defaults, `@react-native-community/datetimepicker@8.4.1` for offline time picking, `src/i18n/ar.ts:1` keys `reminderTitle/morningAdhkarReminder/eveningAdhkarReminder/reminderEnabled/Disabled/pickTime/reminderTime`, and `src/screens/SettingsScreen.tsx:1` themed RTL card `تذكير الأذكار` (two rows: `time-outline` pill `formatNumber` + toggle, `PermissionBlockedBanner` inline when `useNotificationPermissions` denied — toggles disabled + `exactAlarmRationale` footer, `DateTimePicker` is24Hour spinner/default + web fallback, `useTimeGuardedCallback` + `config.interaction.navButtonGuardMs`). Verified `npm run lint:fix`/`npx tsc --noEmit` clean.

- [x] **Schedule Daily Morning/Evening Notifications (#15)** — Added `src/notifications/scheduler.ts:1` (`getNextTriggerDate`/`getNextTriggerTimestamp` pure wall-clock helper, DAILY repeat handles timezone/DST, testable) and extended `src/notifications/notifeeService.ts:1` with `REMINDER_NOTIFICATION_IDS` (`morning-adhkar`/`evening-adhkar`), `createDailyTrigger` (`TriggerType.TIMESTAMP` + `RepeatFrequency.DAILY` + `alarmManager SET_EXACT_AND_ALLOW_WHILE_IDLE`, falls back to WorkManager if exact denied), `cancelReminders`, `scheduleReminders` (permission gate, cancel-before-schedule idempotent, `ensureAdhkarChannel`, stable ids, `ar.morningReminderTitle/Body` `ar.eveningReminderTitle/Body` + `categoryId` 3/4, `data {categoryId,type}`), `rescheduleReminders`. Added `src/i18n/ar.ts:1` keys `morningReminderTitle/eveningReminderTitle/morningReminderBody/eveningReminderBody`. Wired `App.tsx:1` bootstrap (schedule on `appStore` ready, store `subscribe` with `prevRemindersJson` guard, `AppState` `active` reschedule for timezone/reboot). `app.json` already had `RECEIVE_BOOT_COMPLETED` from #13; reboot restores on next launch + `AppState active` (documented OEM limitation). Verified `npm run lint:fix` + `npx tsc --noEmit` clean; offline-first, dedup stable ids, wall-clock not UTC.

- [x] **Background Audio Service (#17)** — Installed `react-native-track-player@4.1.2` (requires prebuild, guarded for web/Expo Go). Added `src/audio/playbackService.ts:1` headless service (RemotePlay/Pause/Stop/Next/Previous/QueueEnded → stop+reset), `src/audio/trackPlayerService.ts:1` (`isTrackPlayerSupported`, `setupPlayer` `appKilledPlaybackBehavior: StopPlaybackAndRemoveNotification` + Play/Pause/Stop caps, `buildTracksForCategory` offline `file://` URIs via `resolveAudioSource`+`resolveLocalAudioUri` + `azkar-index-{id}` + `autoPlayNext`, `playCategory(categoryId)` queue + caps update, `togglePlayPause`/`stopPlayback`/`skipNext/Prev`), guarded `require`. Wired `index.ts:1` `registerPlaybackService` top-level before `registerRootComponent` and `App.tsx:1` `setupPlayer` on store ready; `app.json:18` `ios.UIBackgroundModes: ["audio"]`. Verified `npm run lint:fix`/`npx tsc --noEmit` clean; background/locked playback, `AppKilledPlaybackBehavior` clears notification/service, queue respects `autoPlayNext`.

- [x] **Notification Play Action — تشغيل → TrackPlayer (#16)** — Extended `src/notifications/notifeeService.ts:145` with `REMINDER_PLAY_ACTION_ID='play'` + `actions: [{title:'تشغيل', pressAction:{id:'play', launchActivity:'default'}}]` on daily triggers, added `src/notifications/eventHandler.ts:1` (`handlePlay` debounced 500 ms → `playCategory(categoryId, {startAtCurrentPhrase:true})`, respects `autoPlayNext`, handles missing audio, `handleNotifeeEvent` checks `pressAction.id==='play'` + `data.categoryId`). Wired `index.ts:19` `onBackgroundEvent` top-level headless (killed state, AsyncStorage via `playCategory`, no hooks) and `App.tsx:29` `onForegroundEvent` (foreground/backgrounded) with cleanup. Queue respects `autoPlayNext` single vs full, `TrackPlayer.reset` before play avoids race. Verified `npm run lint:fix`/`npx tsc --noEmit` clean; RTL action title, offline-first, web/Expo Go no crash.

- [x] **Playback Stop/Cleanup + Notification Deduplication (#18)** — Enhanced `src/audio/trackPlayerService.ts:151` `stopPlayback(reason)` idempotent `stop+reset` (clears foreground service + media notification, safe when idle), `src/audio/playbackService.ts:1` `RemoteStop`/`QueueEnded` auto cleanup on natural finish, and `src/notifications/notifeeService.ts:241` helpers `cancelAllReminders`/`cancelMediaNotification`/`pruneStaleDisplayedNotifications` (idempotent, dedup via `cancelTriggerNotifications` stable ids `morning-adhkar`/`evening-adhkar` before reschedule, rapid toggle safe, offline-only). Verified `npm run lint:fix`/`npx tsc --noEmit` clean; no leaked service after Stop, no duplicates, cancel safe when nothing playing.

- [x] **Weekly Friday Reminder — سنن يوم الجمعة (#20)** — Extended `src/store/slices/reminderSlice.ts:1` with `friday {enabled:true, time:{9,0}}` + `setFridayEnabled/toggleFriday/setFridayTime` + `setReminders` migration, 3-place wiring `src/store/persistence.ts:1` (`isAnyOf toggleFriday...` → `adhkarReminders` + `loadPersistedState` friday fallback) + `src/config/config.ts:1` `fridayDefault`, `src/notifications/scheduler.ts:1` `getNextFridayTriggerDate/Timestamp` (Friday=5, 0/7-day logic, wall-clock), `src/notifications/notifeeService.ts:1` `REMINDER_NOTIFICATION_IDS.friday='friday-adhkar'` + `createWeeklyTrigger` (`WEEKLY` + `SET_EXACT_AND_ALLOW_WHILE_IDLE`) + `scheduleReminders` friday weekly with `ar.fridayReminderTitle/Body` `categoryId 21` + `تشغيل` action (`data {categoryId:'21',type:'friday'}`), `src/i18n/ar.ts:1` keys `fridayReminderTitle/Body` `fridayAdhkarReminder`, `src/notifications/eventHandler.ts:1` allows `21` + friday fallback, `src/screens/SettingsScreen.tsx:1` third row in same card (`time-outline` pill `formatNumber` + toggle `guardedToggleFriday`, `PickerTarget` friday, `handleTimeChange` → `setFridayTime`), `App.tsx:1` `RemindersState` typed schedule (daily 06:00/17:00 + weekly Friday 09:00, no conflict both fire same Friday, dedup stable ids). Verified `npm run lint:fix`/`npx tsc --noEmit` clean; RTL/themed, persisted, offline-first.

- [x] **Verify Adhkar Reminders E2E Matrix (#19)** — Created `docs/adhkar-reminders-test.md:1` executable E2E checklist covering Task 7 (scheduled fire wall-clock DAILY/WEEKLY, Play تشغيل in 3 app states via `onForegroundEvent`/`onBackgroundEvent` + `handlePlay` 500 ms dedup + `playCategory` `file://` offline, media controls sync Play/Pause/Stop/Skip + `playbackService` `QueueEnded→stop+reset` + `AppKilledPlaybackBehavior.StopPlaybackAndRemoveNotification` + locked screen, permission-denied banner + gated toggles + precise/imprecise alarm fallback, reboot restore on first launch + `AppState active`, timezone wall-clock recompute, missing audio guard). Verified `npm run lint:fix` + `npx tsc --noEmit` clean; dev build required, Expo Go/web no-ops documented.

- [x] **Web Bundling: shaka-player / TrackPlayer mock** — Fixed `Web Bundling failed: Unable to resolve "shaka-player/dist/shaka-player.ui"` from `react-native-track-player@4.1.2` web `Player.js:40` (`shaka-player` optional peer). Added `metro.config.js:1` (`getDefaultConfig`, `resolver.platforms` web, `resolveRequest` mocks `react-native-track-player` + `shaka-player` on `platform === 'web'` → `src/mocks/empty.js:1`) with `src/mocks/empty.js:1` stub (`TrackPlayerMock`, `Capability`, `State`, `Event`, `AppKilledPlaybackBehavior`, `Player.isBrowserSupported` shim). Verified `npx expo export --platform web` bundles 871 modules (`Web Bundled 22026ms`) vs prior 8146ms failure; `npx tsc --noEmit` clean; native `ios/android` still resolves real TrackPlayer.

- [x] **Web Deprecation Warnings: shadow*/textShadow*/pointerEvents** — Fixed `react-native-web` warnings (`shadow*`→`boxShadow`, `textShadow*`→`textShadow`, `props.pointerEvents`→`style.pointerEvents`) via `Platform.select` per `AGENTS.md`. `src/components/TasbihButton.tsx:1,174,185` (`buttonWrap` `shadow*`/`elevation` → `boxShadow` on web + inline `shadowColor`→`boxShadow` with `theme.tasbihShadowColor`, `label` `textShadow*`→`textShadow`), `src/components/PhraseCard.tsx:1,421,447,272` (`card` `shadow*`→`boxShadow`, `categoryLabel` `textShadow*`→`textShadow`, `LinearGradient overflowFade` `pointerEvents` prop→style), `src/screens/CategoriesScreen.tsx:1,217,90` (`quoteCard` `shadow*`→`boxShadow`, `Animated.View searchWrap` `pointerEvents` prop→style), `src/screens/FreeTasbihScreen.tsx:1,167` (`LinearGradient`, `View card/counterArea/totalChip` `pointerEvents` props→`style.pointerEvents`). Verified `npm run lint`/`npx tsc --noEmit` clean, `npx expo export --platform web` clean, no web console warnings.

- [x] **Settings Screen Redesign (Premium RTL Arabic)** — Redesigned `SettingsScreen` to match iOS-style RTL design spec: warm beige background (#F7EFE2), deep forest green accents (#1E4338), muted olive-gold tones, off-white cards with 20px radius and soft shadows. Reminder Times section (`مواعيد التذكير`) now mirrors top section hierarchy: label (icon + Arabic text) on left, controls (pill time badge + green toggle) on right; removed "مفعل/موقوف" status text. Total Counter (`إجمالي الأذكار`) unified card with trash icon left, label + counter badge right. All borders use theme-appropriate `buttonBorderColor`. Updated solarized theme to exact spec; light/dark themes proportionally adjusted with new shadow tokens.

- [x] **Remove react-native-track-player — Fix Release Build Kotlin Compile** — Build failed `Execution failed for task ':react-native-track-player:compileReleaseKotlin'` (Kotlin 2.1.20 mismatch + `onBind(null)` patch). Removed package entirely to unblock release: deleted `react-native-track-player@4.1.2` from `package.json:50` + `expo.doctor.exclude`, removed `patches/react-native-track-player+4.1.2.patch:1`, `src/audio/trackPlayerService.ts:1` + `src/audio/playbackService.ts:1` + `src/notifications/eventHandler.ts:1` (Play تشغيل handoff), `src/mocks/empty.js:1` + `metro.config.js:1` web mock (reverted to default `getDefaultConfig`), stripped `backgroundAudio` from `src/config/features.ts:13` (`FeatureKey` now `foregroundAudio|reminders|volumeNav`), removed `withPlayAction` تشغيل actions in `src/notifications/notifeeService.ts:163,206`, cleared `index.ts:13` `registerPlaybackService` + `App.tsx:12,35,60` `setupPlayer`/`onForegroundEvent`, removed `app.json:18` `ios.UIBackgroundModes ["audio"]` and `FOREGROUND_SERVICE*` permissions. `AGENTS.md:23` updated (Audio/tech-stack, Feature flags, Notifications, structure, bootstrap, `mocks/` removed). Verified `npm run lint:fix` 0 errors (6 pre-existing `SettingsScreen` warnings), `npx tsc --noEmit` clean, `npx expo export --platform web` 871→871 clean, `npm ls react-native-track-player` empty; foreground `expo-audio` + reminders still offline-first, no network.

- [x] **SettingsScreen Row Contracts & Visual Consistency (#22)** — Standardized the Settings layout contract in `src/screens/SettingsScreen.tsx` so every card follows one pattern (bold title at top, rows below): added the missing `تذكير الأذكار` title inside the Reminder group card (was a standalone tiny `sectionHeader` above the card), added a `إجمالي الأذكار` title to the previously title-less Total Counter card and dropped its duplicated in-row label (trash icon + count badge only), normalized row vertical padding to 10 (`toggleRow` 8→10, `reminderItemRow` 14→10), and removed 11 unused styles (`divider`, `rationaleText`, `sectionHeader`, `infoRow`, `totalCounterLabel`, `contactBtn`, `contactBtnText`, `contactForm`, `input`, `inputMultiline`, `githubRow`, `githubText`). No new components/store/strings; all 5 cards (Theme / Font Size / Settings / Reminders / Total Count) now share the same title+rows contract. Verified `npm run lint:fix` clean, `npx tsc --noEmit` clean, prettier clean; offline-first, no feature change.

- [x] **Light Theme Blue Accent Correction** — Updated the `light` palette in `src/theme/azkarTheme.ts` to the "relaxed shiny blue" accent direction originally documented in the redesign spec: `iconColor`/`sliderBgActive` `#1E4338`→`#3B82F6`, `verseGradient` `['#1E4338','#16352D']`→`['#3B82F6','#1D4ED8']`, `accentGradient` `['#BB9A4F','#9A7D3D']`→`['#9BC6FC','#60A5FA']` (hex normalized to uppercase). Solarized (emerald+gold) and dark (blue) palettes unchanged. Docs synced: `AGENTS.md:159` theaming light direction (dropped "emerald primary"), `.agents/skills/zikrukum-theming/SKILL.md:19` palette note. Verified `npx tsc --noEmit` + `npx eslint src/theme/azkarTheme.ts` clean; offline-first, token-based, no feature change.

- [x] **Notification Toggle ↔ OS Permission Sync (#23)** — Fixed reminder toggles claiming ON while the OS permission is disabled: `src/screens/SettingsScreen.tsx:1` re-checks permission on every focus (`useFocusEffect` + existing AppState refresh), gates toggle-ON through `requestNotificationPermission()` (only `set*Enabled(true)` on `authorized`/`provisional`; post-request `denied` opens system Settings once, no re-prompt loop; OFF always allowed via `set*Enabled(false)`), and renders the effective state (`reminder.enabled && granted`) so switches never claim ON while blocked. Mismatch (`enabled` in Redux but not granted) shows inline `notifMismatchWarning` copy + existing `PermissionBlockedBanner` (Request + Open Settings). `src/notifications/permissions.ts:1` exposes `granted`; `src/i18n/ar.ts:1` adds the RTL mismatch string; theme tokens only, `useTimeGuardedCallback` + `config.interaction.navButtonGuardMs`, Notifee-only, web/Expo Go no-ops preserved. Verified `npm run lint:fix` + `npx tsc --noEmit` clean.

- [x] **Notification Tap Deep-Link to Category (#24)** — Tapping a reminder notification lands on `Category { categoryId }` (payload `data { categoryId, type }`, morning→3 / evening→4 / friday→21, existing route, no new screen). Added `src/navigation/navigationRef.ts:1` (`navigationRef` + `isReadyRef` + pending queue + `navigateToCategory` with bundled-`azkar` id validation, unknown ids fall back to `Categories` instead of the `CategoryScreen:430` infinite loader) and `src/notifications/notificationRouter.ts:1` (`extractCategoryId` string/number-tolerant + `handleNotificationPress` + `pending-notification-category` AsyncStorage backup). Wired `RootNavigator` (`ref` + `onReady` flush), `index.ts` headless `onBackgroundEvent` PRESS persist (never crashes headless JS), `App.tsx` mount effect (`features.reminders` + `isNotifeeSupported` gated: `subscribeForegroundNotificationPress` with cleanup + `getInitialNotificationData` first + backup always consumed/cleared, navigate only when data exists). `notifeeService` gained guarded `subscribeForegroundNotificationPress`/`getInitialNotificationData` (`EventType.PRESS` read at runtime, verified `=1` in 9.1.8 types; payload shape untouched). `AGENTS.md` structure + Navigation prose synced. Verified `npm run lint:fix` + `npx tsc --noEmit` clean; device tap QA (foreground/background/killed cold start) still required on a dev build.

- [x] **Explicit Permission Request with Pre-Permission Rationale (#25)** — First toggle-ON while OS status is `not-determined` shows a one-time themed RTL rationale dialog (`src/components/PermissionRationaleDialog.tsx:1`, `notifRationaleTitle/Body/Continue` keys + existing `cancel`), then the real OS prompt. Continue/Cancel both persist `notifPrePermissionShown` via `src/utils/storage.ts` (`wasRationaleShown`/`markRationaleShown` in `src/notifications/permissions.ts:1`, no Redux slice per scope); real OS state is still re-checked every focus regardless of the flag. Grant dispatches enable + immediate `scheduleReminders()`; plain denied keeps Redux off (banner retry path); post-request denied opens system Settings once, no re-prompt loop. No Android-version branching (Notifee handles 13+ `POST_NOTIFICATIONS`); web/Expo Go no-ops (status is `denied`, rationale never triggers). Verified `npm run lint` zero warnings + `npx tsc --noEmit` clean.

- [x] **Progress Milestone Notifications (#26)** — Immediate local celebrations via Notifee `displayNotification` on the single `adhkar-reminders` channel (stable ids `milestone-<key>`, tap just foregrounds the app — `isMilestonePress` guard in `App.tsx` keeps #24 deep-link from misrouting). New persisted `milestones` slice `src/store/slices/milestonesSlice.ts:1` (`enabled`, fire-once `achieved[]`, `streakCount`, `lastOpenDate`) with full 3-place wiring + preload count-seeding (existing users above a threshold never get backlogged celebrations); firing listeners in `src/store/milestoneListeners.ts:1` (`incrementTotalCount` → 100/1k/10k covering tap/volume/audio-loop incl. FreeTasbih, `completeCategory` → per-category + first-ever special copy + all-categories, `advanceStreak` → 3/7/30/40 + Friday nudge). Helpers in `src/notifications/milestones.ts:1` (copy tables, 23:00–05:00 quiet-hours record-without-display, local-day streak math); `CategoryScreen.tsx:1` dispatches `completeCategory` on natural final-phrase count-through only (resets never count); `App.tsx:1` advances streak once/day on startup + foreground; `SettingsScreen.tsx:1` own toggle reusing the #25 rationale gate + effective-state rendering. Resets never refire (separate slice, achieved-key guards). `AGENTS.md` structure/slice-table/bootstrap synced. Verified `npm run lint` zero warnings + `npx tsc --noEmit` clean; device QA (delivery, streak, cold start) still required on a dev build.

- [x] **Simplify Dhikr Header to Back + Settings (#29)** — `src/components/PhraseCard.tsx:1` header now shows exactly two icons: back chevron (`onBack`, unchanged) + Settings gear (`SimpleLineIcons name="settings"` matching `CategoriesScreen`, guarded via `useTimeGuardedCallback` + `config.interaction.navButtonGuardMs`, existing `settings` i18n key). Deleted reset + font-/font+ buttons and their `fontScale` dispatches; `src/screens/CategoryScreen.tsx:1` drops `handleReset` + `onReset` prop (Back already clears `azkar-index-{id}`). Fixed 40px side slots keep the pill balanced; pager `expectedIndexRef` sync and RTL mirror untouched. Verified `npm run lint` + `npx tsc --noEmit` clean.
- [x] **Lighter Progress Bar Color for Title Contrast (#30)** — New `progressFill` token in `src/theme/azkarTheme.ts:1` (values copied verbatim from existing palette, no new colors): light `#60A5FA`, solarized `#BB9A4F` gold, dark `#1D4ED8`. Measured label-on-fill contrast: 5.77 / ~4.49 / 5.44 (was 3.99 / 1.10 / 2.98); dark deliberately deviates from the issue's lighter-blue guess (1.46–2.07 with the light label). Removed the white `textShadow` glow on the label. Full table posted on the issue. Verified `npm run lint` + `npx tsc --noEmit` clean.
- [x] **Widen Progress Bar After Header Simplification (#31)** — Same `PhraseCard` header block: `headerCenter { flex: 1, marginHorizontal: 4 }` (was 8), `progressTrack width 100%`, side slots shrunk to single 40px slots; 16px outer/card gutters preserved. Title ellipsizes (`numberOfLines={1}`) inside the wider pill; RTL fill still anchored `right: 0`. Verified `npm run lint` + `npx tsc --noEmit` clean; visual check across widths still required on device.